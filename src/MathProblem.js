class MathProblem {
    constructor(numA, operand, numB, userAnswer, problemSolution) {
      this.numA = numA;
      this.operand = operand;
      this.numB = numB;
      this.userAnswer = userAnswer;
      this.problemSolution = problemSolution;
    }
  }
  
  export default MathProblem;
  